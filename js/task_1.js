"use strict";

//Task-1
const name = "protectiveFieldGenerator";
let price = 1000;
console.log(`${name} selected, unit price ${price} credits`);
price = 2000;
console.log(`${name} selected, unit price ${price} credits`);
